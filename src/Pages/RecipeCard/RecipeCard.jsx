import React from 'react';

const RecipeCard = ({ recipeData }) => {
    console.log(recipeData);
    return (
        <div>
            <h2>recipe Card</h2>
        </div>
    );
};

export default RecipeCard;